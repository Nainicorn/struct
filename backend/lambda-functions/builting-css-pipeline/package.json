{
  "name": "builting-css-pipeline",
  "version": "1.0.0",
  "type": "module",
  "description": "Consolidated CSS pipeline: ValidateCSS + RepairCSS + NormalizeGeometry + WallMerge + Openings + Slabs",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
